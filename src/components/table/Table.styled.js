import styled from 'styled-components';

const MarginAutoTable = styled.table`
  margin-left: auto;
  margin-right: auto;
`;

const BoldFontThead = styled.thead`
  font-weight: bold;
`;

const TitleTR = styled.tr`
  height: 2rem;
`;

const ContentTD = styled.td`
  padding: 0.25rem 1rem;
`;

export default {
  MarginAutoTable,
  BoldFontThead,
  TitleTR,
  ContentTD,
};
